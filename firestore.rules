
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for development purposes.
    // In a production app, you would secure this with authentication rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
